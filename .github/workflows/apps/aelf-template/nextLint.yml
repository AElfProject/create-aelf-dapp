name: Next Lint

on: [pull_request]

jobs:
  lint:
    uses: ./.github/workflows/apps/aelf-template/setup-node.yml
    with:
      cache-name: cache-node-modules

    steps:
      - name: Run ESLint
        run: pnpm lint
