name: Next Lint

on: [pull_request]

jobs:
  lint:
    uses: ./.github/workflows/setup-node.yml
    with:
      cache-name: cache-node-modules

    steps:
      - name: Run ESLint
        run: pnpm lint
